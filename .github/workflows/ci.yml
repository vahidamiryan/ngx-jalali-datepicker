name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  build-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install
        run: npm ci

      - name: Build core (@vahidamirian/datepicker-core)
        run: npm run build -w @vahidamirian/datepicker-core

      - name: Test core
        run: npm run test -w @vahidamirian/datepicker-core

      - name: Build Angular (@vahidamirian/ngx-jalali-datepicker)
        run: npx ng build angular

      - name: Test Angular (headless)
        run: npx ng test angular --watch=false --browsers=ChromeHeadless

      - name: Build Vue (@vahidamirian/vue-datepicker)
        run: npm run build -w @vahidamirian/vue-datepicker

      - name: Test Vue
        run: npm run test -w @vahidamirian/vue-datepicker

      - name: Typecheck Vue
        run: npm run typecheck -w @vahidamirian/vue-datepicker

      - name: Build docs
        run: npm run build -w @vahidamirian/docs
