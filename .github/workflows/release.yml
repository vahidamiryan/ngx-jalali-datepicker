name: Release (publish to npm)

# Publishes @vahidamiryan/datepicker-core, @vahidamiryan/ngx-jalali-datepicker and @vahidamiryan/vue-datepicker to npm. Trigger manually and
# choose which packages to publish, or push a tag like v1.2.3 to publish all.
on:
  workflow_dispatch:
    inputs:
      packages:
        description: 'Space-separated packages to publish (default: all)'
        required: false
        default: '@vahidamiryan/datepicker-core @vahidamiryan/ngx-jalali-datepicker @vahidamiryan/vue-datepicker'
  push:
    tags:
      - 'v*'

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write # npm provenance
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          registry-url: 'https://registry.npmjs.org'
          cache: 'npm'

      - name: Install
        run: npm ci

      # Build in dependency order. @vahidamiryan/datepicker-core must be built first because the
      # framework packages resolve its types/JS.
      - name: Build core
        run: npm run build -w @vahidamiryan/datepicker-core
      - name: Build Angular
        run: npx ng build angular
      - name: Build Vue
        run: npm run build -w @vahidamiryan/vue-datepicker

      # Publish. @vahidamiryan/datepicker-core ships from packages/core; @vahidamiryan/ngx-jalali-datepicker from the
      # ng-packagr output in dist/angular; @vahidamiryan/vue-datepicker from packages/vue.
      - name: Publish @vahidamiryan/datepicker-core
        run: npm publish -w @vahidamiryan/datepicker-core --access public --provenance
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Publish @vahidamiryan/ngx-jalali-datepicker
        run: npm publish ./dist/angular --access public --provenance
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Publish @vahidamiryan/vue-datepicker
        run: npm publish -w @vahidamiryan/vue-datepicker --access public --provenance
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
